{
  "tasks": {
    "dev": "deno run -A --unstable https://deno.land/x/ter/main.ts --serve",
    "dev-local": "deno run -A --unstable --watch $HOME/projects/ter/main.ts --serve",
    "build": "deno run -A --unstable https://deno.land/x/ter/main.ts",
    "build-local": "deno run -A --unstable $HOME/projects/ter/main.ts",
    "clean": "rm -rf _site"
  }
}
